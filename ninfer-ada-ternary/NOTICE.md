# NOTICE · 来源、许可与归属

本仓是 **NInfer** 的派生作品（derivative work），并消费第三方模型权重。发布/分发前请阅读本文件。

---

## 1. 引擎与工具（本仓的代码）

- **上游**：`Neroued/ninfer` —— **Apache License 2.0**
- 本仓的引擎侧改动（`patches/`）与打包/验证工具（`tools/`）为在上述作品基础上的修改，
  同样以 **Apache License 2.0** 发布，并保留原始版权与许可声明。
- 依据 Apache-2.0 §4，**已标明修改**：本仓相对上游 NInfer **新增/修改的文件**以**逐文件对照形式**放在
  `patches/changed-files/`（与上游同目录结构，覆盖即可），改动清单、重建步骤与必做验证见
  `patches/README-改动说明.md`；每个改动文件顶部也带 `MODIFIED for the NInfer ternary port` 声明行。

## 2. 模型权重（**本仓不分发**）

本项目消费的模型为 **Ternary Bonsai 2 27B**（底座 `Qwen/Qwen3.8-27B`，架构未改，
权重为 1.58-bit 三元 + Hadamard 旋转基）。

- **权重版权归其原作者/发布方所有**（PrismML 及其上游 Qwen 体系）；
- 本仓**不包含、也不重新分发**模型权重，仅提供**操作方法与工具**；
- 使用者需自行从**官方渠道**获取权重，并遵守其**各自的许可条款**（可能为非 Apache 的社区许可）。
- 由本项目产出的 `.ninfer` 制品属于**权重派生品**，其再分发义务**以权重原许可为准**，与代码许可无关。

> ⚠️ 结论：**代码可自由按其许可使用；权重不可由本仓分发。** 请不要把大体积权重制品随本仓一并发布，除非你已确认原许可允许。

## 3. 第三方引用与灵感

- 三元编解码语义对齐 `ggml-quants.c`（llama.cpp 生态）；
- Hadamard 折叠基的语义参考 PrismML 的公开运行时与 `prism.hadamard.*` 元数据契约；
- 张量核 FWT 的设计思路受公开的 HadaCore / TurboQuant 工作启发。

以上仅作**方法参考**，本仓代码为独立实现。

## 4. 无担保

本仓按 **"原样（AS IS）"** 提供，不附带任何明示或暗示的担保。数字均为特定硬件/软件环境下的一次实测，
不同环境（卡型、驱动、CUDA、内存带宽）会有差异。

---

## 附：发布前脱敏检查清单

发布本仓或其任何派生制品前，**逐项确认**：

- [ ] 仓内**不含**任何 API Key / Token / 口令 / 私钥（`key.txt`、`credentials/session`、`settings.yaml` 等一律排除）；
- [ ] 仓内**不含**个人路径 / 主机名 / 邮箱等可识别信息（用占位符 `<NINFER_ROOT>` 之类替代）；
- [ ] 仓内**不含**大体积模型权重（除非已确认原许可允许再分发）；
- [ ] `README.md` 的 YAML 头 `license` 字段取值在平台白名单内
      （`Apache License 2.0` / `MIT` / `AFL-3.0` / `ECL-2.0` / `GPL-2.0` / `GPL-3.0` / `LGPL-2.1` / `LGPL-3.0`）；
- [ ] 若在魔搭发布：卡片**必须写全**，否则平台按"预发布"处理 ⇒ **仅所有者可见**。
