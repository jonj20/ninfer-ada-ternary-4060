# RTX 4060 8G · Ternary Bonsai 2 PTQ1_0 · Linux 移植开发跟踪

> 本文档跟踪 **RTX 4060（8 GiB，sm_89）** 上跑通 **Ternary Bonsai 2 27B · PTQ1_0** 的开发过程。
> 主线工程：本仓 `ninfer-ada-ternary-3090-4090-linux`。
> 状态字段用：`未开始` / `进行中` / `阻塞` / `完成` / `延期`。阶段完成后回写日期与证据。

---

## 0. 决策摘要（2026-09-23 确认）

| 决策项 | 结论 |
|---|---|
| 主线分支 | **`ninfer-ada-ternary-3090-4090-linux`**（本仓） |
| 目标硬件 | NVIDIA **RTX 4060 8 GiB**，**sm_89**，运行环境 **Linux** |
| 目标格式 | **PTQ1_0**（`PTQ1_0_G128` / `weights_id=folded-ternary`） |
| MTP / vision | **本期不支持**（对象表裁剪，不做联调） |
| 芯片支持 | **允许新增 4060 档位**（构建命名、容量档、文档与 bench 口径） |
| 独立 4060 引擎树 | **不建**。构建/产物用路径与环境变量区分；不与 Ambolio/4070 血统混树 |
| 4070 内核深优化 | **二期**按函数回移 + e2e 回归，不阻塞本期 |

### 为何选本线（对比后放弃的路径）

| 候选 | 结论 | 理由 |
|---|---|---|
| **3090-4090-linux（本仓）** | ✅ 主线 | 唯一完整 Linux 产品线（`just`/`uv`/补丁/转换/e2e）；**PTQ1_0 已 E2E**（391、MMA 双路径、负控）；`pack.py` 已参数化且校验 v2 模板 |
| ninfer-ada-ternary（主线文档包） | ❌ 不用作构建线 | Windows/4080S 血统，路径写死，无一键 Linux 构建 |
| ninfer-ada-ternary-4070-linux | ❌ 二期 | 内核优化最深，但是 Ambolio 补丁快照、无构建体系；与本线基线不同，合树成本高 |
| ninfer-ada-ternary-3090 | ❌ 不用 | bench/研究线，非发行线 |
| ninfer-ada-ternary-4060（Ambolio 拷贝） | ❌ 已删除 | 用户已删；避免双基线 |

**架构说明：** 上游本线仅注册 sm_86 / sm_89。4060 为 sm_89，**无需放宽架构白名单**，`just build`（默认 89）即可。

---

## 1. 资产与路径（已核对）

### 1.1 输入权重

| 资产 | 路径 | 规格 | 校验 |
|---|---|---|---|
| PTQ1_0 GGUF | `D:\LLM\llama\Ternary-Bonsai-2-27B-PTQ1_0.gguf` | 5,946,648,928 B（约 5.54 GB） | 已就位 |
| **groupwise-int 模板 v2** | `D:\LLM\llama\qwen3_8_27b-v2.ninfer` | 20,437,336,576 B | **magic `NINFER\x00\x02`**；`identity={model_id: qwen3.8-27b, weights_id: groupwise-int}`；**1190 objects** = frontend 6 + text 773 + mtp 12 + vision 333 + dflash2 66 |
| 废弃：模板 v3 | ~~`D:\LLM\llama\qwen3_8_27b.ninfer`~~ | 约 19 GB | **不可用**（magic v3，无 Ambolio/linux pack 所需 `identity` 结构）；可删腾盘 |

**模板来源约定（防再下错）：**

```text
# 正确（v2，本线 pack 只认 v1迁移/v2）：
https://huggingface.co/neroued/Qwen3.8-27B-NInfer/resolve/dc370fb6295a/qwen3_8_27b.ninfer

# 错误（main 默认已迁到容器 v3）：
https://huggingface.co/neroued/Qwen3.8-27B-NInfer/resolve/main/qwen3_8_27b.ninfer
```

### 1.2 工程与磁盘

| 项 | 值 | 备注 |
|---|---|---|
| 主线仓 | `...\infer-ternary\ninfer-ada-ternary-3090-4090-linux\` | 补丁 45 文件快照 + pack + verify + Python 包 |
| 上游钉扎 | `UDPSendToFailed/ninfer-4090` **v1.2.0**（`5c60b7c9`） | 由 `build_backend.py` 拉取；sm_86 + sm_89 |
| D 盘空闲（2026-09-23） | **约 9.9 GB** | 构建 + 产物偏紧；**优先删 v3 模板（~19 GB）** 或产物/构建分盘 |
| 构建根默认 | `/data/ninfer-build`（justfile） | 4060 机可用 `NINFER_BUILD_ROOT` 覆盖，建议命名含 `4060` |
| 产物目录默认 | `.../Ternary-Bonsai-2-27B-ninfer/` | 建议子目录或文件名带 `4060` / `ptq1_0-text` |

### 1.3 磁盘预算（PTQ1_0）

| 项 | 约占用 |
|---|---|
| 全量 PTQ1_0 制品（历史实测） | **8.637 GiB**（9,274,212,876 B） |
| 裁掉 vision/mtp 后（预期） | 显著小于全量（借用部分去掉 333+12 对象；text 权重约 **5.52 GiB** 量级） |
| 模板 v2 | 19.03 GB（已占盘，不重复计） |
| 引擎构建树 | 约 0.7–数 GB（临时根 build_backend 会清；`just build` 常驻） |

**行动：** 打包前确认目标盘剩余 **≥ 预期产物 + 1 GB**；v3 模板在 v2 校验通过后删除。

---

## 2. 范围（本期 In / Out）

### In

1. Linux + **sm_89** 编译本线三元引擎  
2. `pack.py`：`--template` + `--gguf`（PTQ1_0）→ **text-only** 制品（去 vision/mtp）  
3. 4060 8G 冒烟：装载、**17×23→391**、MMA 双路径、Hadamard 负控  
4. 容量档：`rk4v4-e8` / 小 `max-context` / `prefill-chunk` 定型  
5. 记录 decode/prefill 一版基线（不调优）  
6. 本跟踪文档 + 必要的 README/justfile 环境变量说明补丁  

### Out（明确不做）

- MTP 投机、vision 塔、mmproj  
- 4070 线旋转打包/small-T 深度回移  
- 权重 offload / 多卡（上游亦不支持）  
- sm_86 以外新架构白名单  
- 权重再分发（遵守上游/PrismML 许可）  

---

## 3. 阶段计划与检查表

### P0 环境与模板 — 状态：`完成`（2026-09-23）

- [x] 模板为 **v2** 且 `weights_id=groupwise-int`（脚本核验通过）  
- [x] GGUF PTQ1_0 路径确认  
- [x] 主线选型写入本文 §0  
- [ ] 4060/Linux 目标机依赖齐：CUDA ≥13.1、cmake≥3.28、ninja、gcc、ffmpeg、libcurl、`uv`、`just`  
  - 状态：`未开始`（依赖目标机）  
- [ ] 删除或归档 v3 模板腾盘  
  - 状态：`未开始`  

### P1 打包改造 — 状态：`完成`（2026-09-23）

**目标：** 在本线 `tools/pack.py` 上支持「不写 vision/mtp」的 text-only 构建，路径全参数化（已有 `--template`/`--gguf`）。

- [x] 增加开关：`--skip-vision` / `--skip-mtp` / `--skip-dflash2`（及 env `NINFER_TERNARY_SKIP_*`）  
- [x] 过滤点：`mode_build` 的 `_drop_filtered()`，按前缀丢 `vision/`、`mtp/`、可选 `dflash2/`  
- [x] **保留** `frontend/*`、全部 `text/*`、`text/hadamard_signs|widths`、`text/draft_head*`；**默认裁** `vision/*`、`mtp/*`、`dflash2/*`（`--keep-*` 可回退）  
- [x] 身份仍写 `folded-ternary`（本线 `OUTPUT_WEIGHTS_ID`）  
- [x] **顺带修复 PTQ1_0 check**：CHECK 2 的 scale 自检原先写死 PQ2_0 的 34 B 块 / offset 0，已按格式分派（PTQ1_0 = 28 B、scale@26）；大张量反量化改分片（`1<<17` 组/片），避免 `token_embd` 一次性 ~4.7 GiB OOM  
- [x] `python tools/pack.py check --template ... --gguf ...` → **`RESULT: OK`**（exit 0）  
- [x] `build --skip-vision --skip-mtp` 产出见 §6  

**验收：** check 全绿；产物 magic v2；对象前缀无 `vision/`、`mtp/` — **已满足**。

**仓内打包快照（2026-09-23）：** `tools/artifact/` 已收进本仓  
（`container.py` 来自 Ambolio v1.0.8；`numeric/layouts/__init` 来自本仓三元补丁）。  
`tools/_bootstrap.py`：无 `NINFER_ROOT` 或指错时回退到本仓 —— **打包不再需要外置 pack-root**。  
编译引擎仍要 `NINFER_ROOT=<完整检出>`。

**一键脚本：**
- `tools/pack_text.py` — check + build + 产物校验；默认裁 **vision/mtp/dflash2**
- `tools/pack_text.sh` — Linux 包装

```bash
# Windows / Linux（路径按本机改）
python tools/pack_text.py \
  --template /path/qwen3_8_27b-v2.ninfer \
  --gguf     /path/Ternary-Bonsai-2-27B-PTQ1_0.gguf \
  --out      /path/out/Ternary-Bonsai-2-27B-PTQ1_0-text.ninfer

# 只自检
python tools/pack_text.py --check-only --template ... --gguf ...

# Linux
tools/pack_text.sh --template ... --gguf ...
```

### P2 构建 sm_89（4060 档） — 状态：`完成`（2026-09-23）

- [x] `just config` 确认路径；按需：`NINFER_BUILD_ROOT=...4060... just build`  
  - WSL2 Ubuntu 22.04，`NINFER_BUILD_ROOT=/home/jon/build_4060`，Release / `CMAKE_CUDA_ARCHITECTURES=89`
- [ ] `just build-tests && just ctest`（有 GPU 时）——未跑，见 §6.3 备注
- [ ] 可选：`just oracle` 旋转 oracle——未做
- [x] 记录：CUDA 13.1 / gcc / cmake 4.4.3；`NINJA_EXIT=0`；cuobjdump 确认 `arch=sm_89`

**验收：** `apps/ninfer --help` 正常；sm_89 cubin 确认；产物 `/home/jon/build_4060/apps/ninfer`（235 MB）与 `apps/ninfer-serve`（246 MB）。  
**注意：** 7.6 GiB 内存机单文件编译会 OOM（`gqa_attention_decode.cu` 峰值超 7.6G）；加 8G swap + `-j1` 编过。ffmpeg 用 BtbN 6.1 预编译包 + pkg-config；cmake 用 kitware jammy 源 4.4.3。

### P3 转换 PTQ1_0 text-only — 状态：`完成`（2026-09-23，制品最终定型）

推荐入口 **`tools/pack_text.py`**（内含 check → build → 校验 magic/identity/前缀/完整性）：

```bash
python tools/pack_text.py --check-only --template ... --gguf ...
# Linux
tools/pack_text.sh --template ... --gguf ...
```

等价底层（可选）：

```bash
python tools/pack.py check --template ... --gguf ...
python tools/pack.py build <out>.ninfer \
  --template ... --gguf ... --skip-vision --skip-mtp --skip-dflash2
```

- [x] check OK  
- [x] build 成功；回写 §6 尺寸表  
- [ ] 可选：`tools/verify/check_row_order.py` / `check_signs.py` / `list_objects.py` 抽检  

**最终制品（P4 定型，2026-09-23）：** `--keep-vision --keep-mtp`、裁 dflash2  
> 绑定实测（R1）要求 mtp/vision **必选存在**、仅 dflash2 可选，见 P4 结论。早期 text-only 裁掉 mtp/vision 的制品启动即报 `required artifact object is missing`，已弃。

**产物路径：** `/mnt/e/gguf/qwen3.6-35b-a3b/Ternary-Bonsai-2-27B-PTQ1_0-text.ninfer`  

**一键脚本（已验收 2026-09-23）：** 仓根 `pack_ptq1_4060.bat`  
- 默认 `D:\dev\python`（≥3.10）、默认路径、裁 dflash2（`--keep-mtp --keep-vision`）；WSL 用 `pack_wsl.sh`（conda lerobot 3.10 + torch）
- 实测产物 **7,047,407,628 B ≈ 6.56 GiB**（frontend 6 + text 775 + mtp 12 + vision 333 = 1126 对象，无 dflash2）

### P4 4060 8G 正确性冒烟 — 状态：`完成`（2026-09-23）

参考本线 `tools/verify/e2e_ternary.sh` 与移植报告 §4.7：

| 用例 | 期望 | 结果 |
|---|---|---|
| 贪心 `17 * 23` | **391** | **✅ 391** |
| `NINFER_TERNARY_MMA=1` vs `=0` | token 序列逐字节一致 | **✅ hash `b327e287e849`（c128/c1024 × mma1/0 四例全一致）** |
| `NINFER_TERNARY_HADAMARD=0` | 乱码（负控分离） | **✅ hash `b0decbd6b552` ≠ 正控** |
| 装载显存 | 权重+workspace+KV+ctx **≤ 约 7.5 GiB**（8G 卡） | **✅ 权重 5.52 GiB / 5.52 GiB，峰值见 §6.4** |
| 无 vision/mtp 引用错误 | 不因缺对象启动失败 | **✅（保留 mtp+vision 后正常装载）** |

> **制品绑定结论（R1 实测）：** 引擎绑定层把 mtp/vision 当**必选存在项**——`features.mtp()/vision()` 只决定 Device vs ValidateOnly，两者都要求对象存在；唯一真正可选的是 dflash2（`binder.has_object("dflash2/feature_projection")` 才 bind）。所以 text-only 制品必须**保留 mtp + vision，可裁 dflash2**。8G 起跑：text+mtp+vision 6.56 GiB + KV/workspace ≈ 7.0 GiB 仍放得下。
>
> **MTP 投机实测（Out 范围确认）：** `--spec mtp` 在 2048 ctx 下 `requested 924MB runtime but only 702MB available`——8G 卡本期不支持投机，与 §2 Out 一致。

建议起跑参数（沿用）：
```bash
NINFER_CLI=<build>/apps/ninfer \
  <artifact.ninfer> \
  --prompt "What is 17 * 23? Answer with just the number." \
  --max-new 16 --max-context 512 --no-thinking --greedy \
  --kv-dtype rk4v4-e8 --seed 1234
```

- [x] 正控 391  
- [x] MMA 一致  
- [x] 负控乱码  
- [x] 记录 `nvidia-smi` / 引擎 MemorySummary（§6.4）  

### P5 容量定型与基线性能 — 状态：`未开始`

- [ ] 推荐档写入 README 或本文（示例）：  
  `--kv-dtype rk4v4-e8 --max-context 4096..8192 --prefill-chunk 1024 --kv-capacity auto`  
- [ ] `just bench <artifact>` 或等价：记录 decode / prefill 一版  
- [ ] 对照参考（非 4060）：4090 上 PTQ1_0 约 **15.8 tok/s** decode（移植报告）；4060 预期更低，只记录不优化  
- [ ] 8G 上下文实测上限（如 4k/8k 是否还能起）  

### P6 收尾 — 状态：`未开始`

- [ ] 回写本表全阶段状态与日期  
- [ ] README 增加「4060 / PTQ1_0 text-only」一小节（或链到本文）  
- [ ] justfile：若需要，增加 `pack-ptq1-4060` 之类配方（可选）  
- [ ] 明确许可与权重不随仓分发声明（已有 NOTICE 口径）  

### 二期（延期）

| 项 | 说明 |
|---|---|
| MTP / vision | 另开需求；模板对象可再纳入 |
| 4070 内核回移 | 按函数合入 + `just e2e`；注意 4060 仅 24 SM |
| PQ2_0 对照包 | VRAM 允许时再打，decode 更快 |
| dflash2 对象策略 | 见 §5 |

---

## 4. 关键命令速查（本线）

```bash
# 路径与开关
just config

# 依赖
just deps
# just deps-install   # Rocky 10，需 root

# 构建 sm_89（4060）
just build

# 测试 / oracle / 端到端
just build-tests && just ctest
just oracle
just e2e <artifact.ninfer>

# 打包（参数按 P3；justfile 亦有 just pack 配方，注意其默认 GGUF 名）
just pack PQ2_0   # 既有配方；PTQ1_0 以 tools/pack.py 显式 --gguf 为准

# 一键（拉上游→补丁→编译→测试→打包）
just from-scratch PQ2_0
```

环境变量（justfile 已 export）：`NINFER_ROOT`、`NINFER_BUILD_ROOT`、`NINFER_CLI`、`NINFER_TERNARY_TEMPLATE`、`NINFER_TERNARY_GGUF_DIR`、`NINFER_TERNARY_ARTIFACT_DIR`、`PYTHON`。

---

## 5. 待决 / 风险登记

| ID | 项 | 影响 | 计划 |
|---|---|---|---|
| R1 | 裁 vision/mtp 后 engine 绑定是否仍要求对象存在 | 可能启动失败 | **已实测：mtp/vision 必选存在（ValidateOnly 也要求对象）；dflash2 可选。** 制品保留 mtp+vision、裁 dflash2 |
| R2 | 模板含 **dflash2/\* 66**；是否一并裁 | 体积/绑定 | **2026-09-23 已定：默认裁**（`pack_text.py` 默认 skip vision/mtp/**dflash2**）；`--keep-dflash2` 可回退。旧 7.942 GiB 制品仍含 dflash2，需用新脚本重打 |
| R3 | `text/draft_head*` 无 GGUF 源，靠模板借 | 缺则对象表声明悬空 | **保留在清单并借用**（体积小）；不做投机也可不启用 MTP 路径 |
| R4 | D 盘仅 ~9.9 GB | build 或 pack 写失败 | 先删 v3；构建根指向空间更大的盘 |
| R5 | `just pack` 默认模板/GGUF 路径指向 `/data/...` | 本机 D:\ 路径不一致 | P3 一律显式 `--template`/`--gguf` 或 export |
| R6 | 4060 未在上游 CI 验证 | 冒烟才能定 | P4 强制门槛 |
| R7 | PTQ1_0 解码慢（base-3） | 体验 | 本期接受；记录基线即可 |

---

## 6. 实测记录（阶段完成后回填）

### 6.1 模板 / GGUF

| 项 | 值 | 日期 |
|---|---|---|
| 模板 magic / weights_id / n_objects | `NINFER\x00\x02` / `groupwise-int` / 1190 | 2026-09-23 |
| GGUF 字节 | 5,946,648,928 | 2026-09-23 |
| GGUF magic / n_tensors | `GGUF` v3 / 851 | 2026-09-23 |

### 6.2 pack check / build

| 项 | 值 | 日期 |
|---|---|---|
| check RESULT | **OK**（exit 0；CHECK1/2/3 全过，含 `token_embd` 分片 decode） | 2026-09-23 |
| 产物路径 | `/mnt/e/gguf/qwen3.6-35b-a3b/Ternary-Bonsai-2-27B-PTQ1_0-text.ninfer` | 2026-09-23 |
| 产物字节 / GiB | **7,047,407,628 B = 6.563 GiB**（与目录声明 need_size 逐字节一致） | 2026-09-23 |
| identity | `qwen3.8-27b` / **`folded-ternary`** | 2026-09-23 |
| objects 数量与前缀分布 | **1126** = frontend 6 + text **775** + mtp 12 + vision 333；**无 dflash2** | 2026-09-23 |
| text produced | 5,930,360,844 B = **5.523 GiB** | 2026-09-23 |
| borrowed 分解 | mtp 12 + vision 333 对象从模板借 payload（本产物 0.344 GiB borrowed 部分见 pack 输出 `{frontend:6, text:2}` + 池化） | 2026-09-23 |
| skipped (filters) | `{dflash2:66}` | 2026-09-23 |
| 完整性 | `payload_off+max(offset+bytes) == filesize` → **True** | 2026-09-23 |

> 注：早期 text-only 制品（裁 mtp/vision）与含 dflash2 旧包均已废弃。**P4 定型：保留 mtp+vision、裁 dflash2**，原因见 P4 绑定结论。

### 6.3 构建

| 项 | 值 | 日期 |
|---|---|---|
| CUDA / gcc / cmake | CUDA 13.1（`/usr/local/cuda-13.1`）/ gcc 11 / cmake 4.4.3（kitware jammy） | 2026-09-23 |
| NINFER_ARCH | 89（`CMAKE_CUDA_ARCHITECTURES=89`，cuobjdump 确认 `arch=sm_89`） | 2026-09-23 |
| NINJA_EXIT | **0**；产物 `/home/jon/build_4060/apps/ninfer`（235 MB）与 `ninfer-serve`（246 MB） | 2026-09-23 |
| ctest | 未跑（7.6G 内存机，编译已 OOM 极限；P4 真机冒烟替代验收） | 2026-09-23 |

### 6.4 4060 冒烟

| 项 | 期望 | 实测 | 日期 |
|---|---|---|---|
| 17*23 | 391 | ✅ **391**（greedy/seed1234/rk4v4-e8/ctx512） | 2026-09-23 |
| MMA 0/1 | 一致 | ✅ hash `b327e287e849`（c128/c1024 × mma1/0 四例逐字节一致） | 2026-09-23 |
| HADAMARD=0 | 乱码 | ✅ hash `b0decbd6b552`（≠ 正控，负控分离） | 2026-09-23 |
| 峰值显存 | 记录 | 权重 5.52 GiB；`gpu weights used 5.52 GiB / 5.52 GiB`；mtp 投机 924 MiB 预留失败（仅 702 MiB 可用）→ 本期不做投机 | 2026-09-23 |
| 能起的最大 max-context（rk4v4-e8） | 记录 | 冒烟用 512 稳；容量定型见 P5 | 2026-09-23 |
| 装载耗时 | 记录 | 权重 H2D 62.6 s；engine construction 69.0 s | 2026-09-23 |

### 6.5 性能基线

| 项 | 值 | 日期 |
|---|---|---|
| decode tok/s（参数） | **3.36**（greedy/rk4v4-e8/ctx2048/max-new200 稳稳态，生成 186 tok，无启动开销） | 2026-09-23 |
| prefill tok/s（参数） | **9.09**（同上；prompt 28 tok） | 2026-09-23 |
| 备注 | 与 max-new16（3.34）一致 ⇒ 非启动开销，真实解速。4090 PTQ1_0=15.8 → 带宽 1/3.7 + SM 1/5 缩水，属硬件天花板；**llama.cpp 同卡同 PTQ1_0 GGUF 见用户实测 25 t/s（待复核同一基准）**。P5 后续若需要，可加 PTQ1_0 GEMV/MMA 快路径（二期）。权重装载 ~63 s 主要在 `/mnt/e` 9p 读盘 |

### 6.5b llama.cpp 对照（已确认同一文件）

| 项 | 值 | 日期 |
|---|---|---|
| llama.cpp（同机 4060、**同一 PTQ1_0 GGUF 文件**，用户实跑） | **25 t/s** | 2026-09-23 |
| ninfer（同机、同模型、同量化内容） | **3.36 tok/s**（稳态） | 2026-09-23 |
| 差距 | **≈ ×7.4，ninfer 明显未发挥该格式效能** | 2026-09-23 |
| 结论（暂行） | 排除了「基准不同」；llama.cpp 对 PTQ1_0 有高效解码路径，ninfer 的 base-3 除模慢路径 + 无 GEMV/MMA 快指令是主因。**这是首要优化项，非本期 P 段范围，转 §9 优化跟踪** |

### 6.6 MTP 投机（Out·记录）

| 项 | 值 | 日期 |
|---|---|---|
| `--spec mtp` 2048ctx | `requested 924MiB runtime but only 702MiB available` → 8G 卡不支持投机 | 2026-09-23 |

---

## 7. 专题：RTX 4060 与 4090 代码层兼容性

> 结论先行：**同一 sm_89 二进制路径，代码层兼容面一致**；差异在显存容量、SM/带宽与调优数字，不在「4060 不支持」。

### 7.1 架构与编译

| 项 | RTX 4060 8G | RTX 4090 24G |
|---|---|---|
| Compute Capability | **sm_89** | **sm_89** |
| 根 `CMakeLists` 白名单 | 允许 `86\|89` | 同 |
| 编译宏 | `NINFER_SM89=1` | 同 |
| 三元 / 通用 CUDA 内核 | **同一套 cubin** | 同 |
| 上游架构扩展 | 无需放宽（本就含 89） | 同 |

4060 与 4090 **同为 Ada sm_89**：编译目标、指令集、内核模板完全相同，不存在「4090 专属 kernel」或 4060 缺指令的分叉。

### 7.2 真实差异维度

| 维度 | 4060 8G | 4090 24G | 性质 |
|---|---|---|---|
| 显存 | **8 GB** | 24 GB | **硬约束**：全量 PTQ1_0 ~8.6 GiB 装不下；须 text-only + 收紧 ctx/KV |
| SM 数 | 24 | 128 | **性能**（占用/并行） |
| 显存带宽 | ~272 GB/s | ~1008 GB/s | **性能** |
| 默认容量档 | 必须收紧 | 可开很大 | **配置**，非架构 bug |
| WDDM / DirectStorage | Linux 下 `#ifdef _WIN32` 不走 | 同（Windows 才有） | 平台差异，与卡型号无关 |
| 内核注释里的调优数字 | 按大卡（如 66 SM）标定 | 主测卡 | **手感/性能**，带边界检查，功能仍正确 |

### 7.3 不在「4060/4090 互比」范围内的不兼容

- **sm_86（3090）**：另一档宏 `NINFER_SM86`，本仓以 89 为主。  
- **sm_120（5090 等）**：白名单拒绝。  
- **Windows-only** 特性（WDDM 预算、DirectStorage）：Linux 构建编不到，与 4060/4090 无关。

### 7.4 判定

| 问题 | 答案 |
|---|---|
| 4090 代码层是否完全兼容？ | **是**（本线主测卡，文档/bench 按它写）。 |
| 4060 代码层是否兼容？ | **是（同一 sm_89 路径）**；风险在 **8G 显存预算 + 未在真机冒烟（P4）**，不在 ISA/架构。 |
| 差异是否只是性能？ | **主体是性能与显存配置**；另加「按大卡标定的启动参数默认值」需 4060 收紧。 |

---

## 8. 专题：Bonsai 2 PQ2_0 vs PTQ1_0

> 结论先行：两种格式**并列支持、正确性都有 E2E**；PTQ1_0 **更省显存、解码更慢**，且**吃不满**当前为 PQ2_0 做的 GEMV/MMA 快路径。本期主力 PTQ1_0 是对的（8G），需接受速度并完成 P4。

### 8.1 格式与存储

| | **PQ2_0** | **PTQ1_0** |
|---|---|---|
| 码 | 2-bit 槽，4 码/字节 | **base-3 三值**，5 码/字节 |
| 每组 128 权重 | base **32 B** + scale 2 B = **34 B** | base **24 B** + high **2 B** + scale 2 B = **28 B** |
| 高位平面 | **无** | **有**（`qh`，权 120..127） |
| 解码 | 移位取码 `(c-1)*d` | **除/取模**，stages `{32,16,8}` |
| 权重例（`[248320,5120]`） | 337,715,200 B | **278,118,400 B（约省 17%）** |
| 业界 bpw | ~**2.125** | ~**1.75**（更省） |

引擎内是**两套独立 `decode_one`**（`ternary_rowsplit_storage.cuh`）；row 几何按 `qtype` 分派（`ternary_row_view.h`：PTQ1=24+2，PQ2=32+0）。**不是同一套码。**

### 8.2 体积与性能（4090，移植报告）

| | PQ2_0 | PTQ1_0 |
|---|---|---|
| 全量制品 | 9.810 GiB | **8.637 GiB** |
| 权重 | 6.70 GiB | **5.52 GiB** |
| decode | **52.3 tok/s** | **15.8 tok/s（约慢 3.3×）** |
| 慢因 | — | base-3 除模 + 高位平面（报告判「符合预期」） |

### 8.3 内核路径差异（为何 PTQ1 更慢不止解码）

| 路径 | PQ2_0 | PTQ1_0 |
|---|---|---|
| T=1 专用 GEMV | **有**（`ternary_pq2_gemv`；`qhigh==nullptr` 才准入） | 走**通用** reference 路径（有 `qhigh`） |
| small-T MMA（T=2..8） | **有**（`mma_admits` 仅 PQ2_0） | **无** → 通用 tile |
| 折叠 Hadamard 旋转 | 同 | 同 |
| `folded-ternary` 身份/容量档 | 同 | 同 |

`ternary_rowsplit_gemm.cu` 明确：GEMV 快路径与 small-T MMA 的准入条件含 `qtype == PQ2_0_G128 && qhigh == nullptr`。

### 8.4 正确性验证 —— PTQ1_0 **不是**「没验过」

| 检查 | PQ2_0 | PTQ1_0 | 出处 |
|---|---|---|---|
| `17*23` → **391** | ✅ | ✅ | 移植报告 §4.7 |
| MMA=1/0 正控摘要 | `9ab7f3dfe667` | **同一摘要** | §4.7 / §一致性 |
| `HADAMARD=0` 负控 | `2c037e1df050` | `0e7f23175819` | 分离即正确 |
| pack `check` | 原生 | **本仓已修几何/分片后 OK** | P1 |
| MTP 接受率（有投机时） | 76.9% | 74.4% | 报告回归段 |

**「文档多写 PQ2_0」的原因：** `just from-scratch`、bench 默认、部分 KV 矩阵举例用 PQ2_0（更快）；**正控摘要两种格式相同**（模型数学一致），负控不同（解码路径不同）——同一套 e2e 判据两边都成立。PTQ1_0 的解码正确性、旋转负控、尺寸反验在移植报告中均有行。

### 8.5 对「主力 PTQ1_0 + 4060 8G」的含义

| 维度 | 结论 |
|---|---|
| 正确性 | 已有 E2E/负控/pack check；**可用** |
| 8G 适配 | PTQ1_0 **更合适**（权重 5.52 vs 6.70；text-only 约 5.87 GiB） |
| 速度 | 明确更慢；4060 预期低于 4090 的 15.8 tok/s |
| 快路径 | 当前 PTQ1_0 **无** PQ2_0 专用 GEMV/MMA → 二期可选优化 |
| 风险 | **R7** 已登记：接受 base-3 慢；**P4 真机冒烟**仍是门槛 |

### 8.6 一句话

PTQ1_0 与 PQ2_0 是**并列支持的两套格式**，正确性**两边都验过**（同一正控摘要）；差异是 **更省显存、解码约慢 3×、且暂无 PQ2_0 快路径**。4060 主力用 PTQ1_0 合理，先完成 P4，速度接受基线或二期补 PTQ1_0 快路径。

---

## 9. 关联文档

| 文档 | 用途 |
|---|---|
| [移植报告-ninfer-4090.md](./移植报告-ninfer-4090.md) | PTQ1_0/PQ2_0 E2E、容器 v2 钉扎、容量查询 |
| [权重档案与容量规划.md](./权重档案与容量规划.md) | workspace ≈ 176712 × min(ctx, chunk) |
| [uv-工具安装.md](./uv-工具安装.md) | `uv tool install` / 环境变量 |
| [依赖安装-RockyLinux10.md](./依赖安装-RockyLinux10.md) | 系统依赖 |
| 仓根 `README.md` | 转换与跑分入口 |
| `tools/pack.py` | 打包器与 `--template` 校验 |
| `tools/verify/e2e_ternary.sh` | 一致性矩阵 |

---

### 9.1 优化跟踪：PTQ1_0 decode 效能（2026-09-23 立案）

> **待办事项，不属于本期 P0–P6 验收范围；作为二期优先项。**

**结论（暂行）：**
- 同机 4060、**同一 PTQ1_0 GGUF 文件**：llama.cpp **25 t/s** vs ninfer **3.36 tok/s**（稳态），差距 ≈ **×7.4**。
- 判定：ninfer 未有效发挥该格式硬件效能；主要嫌疑为 **PTQ1_0 base-3 除模 slow path**（与 PQ2_0 为两套 `decode_one`，PTQ1_0 无 GEMV/MMA 快指令）。

**下一步备选方向（待评估，未定优先级）：**
1. 对比引擎内 PQ2_0 快路径（4090 上 52.3 vs 15.8），确认 PTQ1_0 是否可复用同类 GEMV/MMA 指令（bmatrix/bf16 累加 + 三元映射表）。
2. 剖析 llama.cpp 对该 GGUF 的解码路径（ggml 三元 GEMV 是否走 `dp2a`/`mma` 指令），对齐指令选择。
3. 用 `ncu` 抓 ninfer PTQ1_0 decode 内核占用与带宽利用率，确认是带宽受限还是指令吞吐受限，定位具体瓶颈行。
4. 检查 `--prefill-chunk` 对 decode 的影响（应无），排除 chunk 相关伪劣化。
5. MTP 投机受 8G 显存限制（924 MiB 预留不够），不列入本优化。

**完成后回填：** 实测对比（tok/s ×2 引擎）、根因定位、改动点、复测九宫格。

---

## 10. 变更日志

| 日期 | 变更 |
|---|---|
| 2026-09-23 | 建档：选定 3090-4090-linux 主线；确认 v2 模板与 PTQ1_0 GGUF；写入阶段 P0–P6 与风险表；明确本期不做 MTP/vision、允许 4060 档位命名 |
| 2026-09-23 | P1/P3：`pack.py` 增加 skip 开关并修 PTQ1_0 check（块几何 + 分片反量化）；`check`→OK；`build --skip-vision --skip-mtp`→7.942 GiB text-only 制品（含 dflash2，后删） |
| 2026-09-23 | 默认再裁 **dflash2**；`tools/artifact` 收进仓 + `_bootstrap` 回退，去掉 pack-root；交付 `pack_text.py` / `pack_text.sh` / 根目录 **`pack_ptq1_4060.bat`**（自动选 Python≥3.10）；**ninfer 生成一步完成** |
| 2026-09-23 | 源码合入后：`build_4060.sh/.bat`、`just` 默认仓根/`build_4060`、wheel 树内 cmake（`5623644`）；**§7 4060/4090 兼容性、§8 PQ2_0 vs PTQ1_0 专题报告入库** |
| 2026-09-23 | **P2 完成**：WSL2 sm_89 构建成功（CUDA 13.1 + 8G swap + `-j1`），NINJA_EXIT=0，`--help`/sm_89 cubin 验证通过 |
| 2026-09-23 | **P3 定型 + P4 完成**：制品定格「保留 mtp+vision、裁 dflash2」（6.563 GiB / 1126 对象；R1 实测 mtp/vision 绑定必选、dflash2 可选）；4060 冒烟 **391 ✅**、MMA 0/1 逐字节一致、HADAMARD=0 负控分离；MTP 投机因 8G 显存不足排除（Out 一致） |
| 2026-09-23 | 容量测定 + 推理功能确认：max-context **3584**（kv-explicit，auto 因 1GiB headroom 失败）；鲁迅冰激凌/三星三星/8 水果等提示下模型理解与多轮均正常；P5 建档**稳态 decode 3.36 tok/s**；**llama.cpp 同文件 25 t/s 对照 → ×7.4 差距立案，转 §9.1 优化跟踪** |
