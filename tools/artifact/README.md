# 本目录 = 可独立打包用的 tools/artifact 快照（Windows/Linux 共用）。
# container.py  来自 Ambolio/ninfer-4090-windows @6eb70a0（上游未改，v2 容器）。
# numeric/layouts/__init 来自本仓 patches/changed-files/tools/artifact（三元注册）。
# 引擎若设 NINFER_ROOT，仍以检出树为准（见 tools/_bootstrap.py）。
